/**
 * Curated icon set — the ONLY place that imports from the icon library.
 * Swap `lucide-react` → `lucide-react-native` here in the mobile phase and the
 * whole app's icons move with one edit. Named exports keep tree-shaking intact.
 */
export {
  ShoppingCart,
  Plus,
  Minus,
  Trash2,
  Pencil,
  ChevronRight,
  ChevronLeft,
  ArrowLeft,
  Gift,
  Trophy,
  Sparkles,
  Star,
  Home,
  Receipt,
  User,
  Wallet,
  Ticket,
  Check,
  X,
  Clock,
  MapPin,
  QrCode,
  CreditCard,
  Utensils,
  PartyPopper,
  Search,
} from "lucide-react";
export type { LucideIcon } from "lucide-react";
